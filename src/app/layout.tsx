import './globals.css';

// This is a minimal root layout. The actual layout with i18n
// support is in [locale]/layout.tsx
export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
